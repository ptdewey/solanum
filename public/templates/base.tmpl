{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="description" content="Your personal RSS feed reader on AT Protocol">
    <meta name="theme-color" content="#0a1410">
    <title>{{block "title" .}}Solanum{{end}}</title>
    <link rel="icon" href="/static/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/static/icon-192.svg">
    <link rel="manifest" href="/static/manifest.json">
    <link rel="stylesheet" href="/static/feeder.css?v=2">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js" integrity="sha384-/TgkGk7p307TH7EXJDuUlgG3Ce1UVolAOFopFekQkkXihi5u/6OCvVKyz1W+idaz" crossorigin="anonymous"></script>
    <script src="/static/sw-register.js"></script>
</head>
<body>
    {{if .Session}}
    <div class="app-wrapper">
    <!-- Desktop Sidebar Navigation -->
    <nav class="sidebar-nav">
        <div class="sidebar-content">
            <a href="/" class="sidebar-brand">🌿 Solanum</a>
            
            <div class="sidebar-links">
                <a href="/" class="sidebar-link" data-page="home">
                    <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                        <polyline points="9 22 9 12 15 12 15 22"></polyline>
                    </svg>
                    <span>Home</span>
                </a>
                
                <a href="/feeds" class="sidebar-link" data-page="feeds">
                    <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M4 11a9 9 0 0 1 9 9"></path>
                        <path d="M4 4a16 16 0 0 1 16 16"></path>
                        <circle cx="5" cy="19" r="1"></circle>
                    </svg>
                    <span>Feeds</span>
                </a>
                
                <a href="/reading-list" class="sidebar-link" data-page="reading">
                    <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
                        <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
                    </svg>
                    <span>Reading List</span>
                </a>
                
                <a href="/profile" class="sidebar-link" data-page="profile">
                    {{if .Session.Avatar}}
                    <img src="{{.Session.Avatar}}" alt="Profile" class="sidebar-avatar" data-avatar-cache="desktop" data-avatar-url="{{.Session.Avatar}}">
                    {{else}}
                    <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                        <circle cx="12" cy="7" r="4"></circle>
                    </svg>
                    {{end}}
                    <span>Profile</span>
                </a>
            </div>
        </div>
    </nav>
    
    <!-- Mobile Top Navigation (shown only on mobile) -->
    <nav class="mobile-top-nav">
        <div class="container">
            <div class="nav-content">
                <a href="/" class="nav-brand">🌿 Solanum</a>
            </div>
        </div>
    </nav>
    
    <!-- Mobile Bottom Navigation -->
    <div class="mobile-bottom-nav">
        <div class="mobile-nav-items">
            <a href="/" class="mobile-nav-item" data-page="home">
                <svg class="mobile-nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                </svg>
            </a>
            <a href="/feeds" class="mobile-nav-item" data-page="feeds">
                <svg class="mobile-nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M4 11a9 9 0 0 1 9 9"></path>
                    <path d="M4 4a16 16 0 0 1 16 16"></path>
                    <circle cx="5" cy="19" r="1"></circle>
                </svg>
            </a>
            <a href="/reading-list" class="mobile-nav-item" data-page="reading">
                <svg class="mobile-nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
                    <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
                </svg>
            </a>
            <a href="/profile" class="mobile-nav-item" data-page="profile">
                {{if .Session.Avatar}}
                <img src="{{.Session.Avatar}}" alt="Profile" class="mobile-nav-avatar" data-avatar-cache="mobile" data-avatar-url="{{.Session.Avatar}}">
                {{else}}
                <svg class="mobile-nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                    <circle cx="12" cy="7" r="4"></circle>
                </svg>
                {{end}}
            </a>
        </div>
    </div>
    
    <main class="container">
        {{block "content" .}}{{end}}
    </main>
    
    </div>
    {{else}}
    <main class="container">
        {{block "content" .}}{{end}}
    </main>
    {{end}}
    
    <script>
        // Avatar caching system
        (function() {
            const CACHE_KEY_PREFIX = 'solanum_avatar_';
            const CACHE_EXPIRY_MS = 24 * 60 * 60 * 1000; // 24 hours
            
            // Get cached avatar data
            function getCachedAvatar(url) {
                try {
                    const cached = localStorage.getItem(CACHE_KEY_PREFIX + url);
                    if (!cached) return null;
                    
                    const data = JSON.parse(cached);
                    // Check if cache is expired
                    if (Date.now() - data.timestamp > CACHE_EXPIRY_MS) {
                        localStorage.removeItem(CACHE_KEY_PREFIX + url);
                        return null;
                    }
                    
                    return data.dataUrl;
                } catch (e) {
                    console.error('Error reading avatar cache:', e);
                    return null;
                }
            }
            
            // Cache avatar as data URL
            function cacheAvatar(url, dataUrl) {
                try {
                    const data = {
                        dataUrl: dataUrl,
                        timestamp: Date.now()
                    };
                    localStorage.setItem(CACHE_KEY_PREFIX + url, JSON.stringify(data));
                } catch (e) {
                    // localStorage might be full or disabled
                    console.warn('Could not cache avatar:', e);
                }
            }
            
            // Convert image URL to data URL
            function imageToDataUrl(url, callback) {
                const img = new Image();
                img.crossOrigin = 'Anonymous';
                
                img.onload = function() {
                    try {
                        const canvas = document.createElement('canvas');
                        canvas.width = img.width;
                        canvas.height = img.height;
                        
                        const ctx = canvas.getContext('2d');
                        ctx.drawImage(img, 0, 0);
                        
                        const dataUrl = canvas.toDataURL('image/png');
                        callback(dataUrl);
                    } catch (e) {
                        console.warn('Could not convert image to data URL:', e);
                        callback(null);
                    }
                };
                
                img.onerror = function() {
                    console.warn('Failed to load avatar image:', url);
                    callback(null);
                };
                
                img.src = url;
            }
            
            // Process all avatar images
            function processAvatars() {
                const avatars = document.querySelectorAll('[data-avatar-cache]');
                
                avatars.forEach(function(img) {
                    const url = img.getAttribute('data-avatar-url');
                    if (!url) return;
                    
                    // Try to load from cache first
                    const cached = getCachedAvatar(url);
                    if (cached) {
                        // Use cached version immediately to prevent flicker
                        img.src = cached;
                    }
                    
                    // Fetch and cache the image in the background
                    // This ensures the cache stays fresh
                    imageToDataUrl(url, function(dataUrl) {
                        if (dataUrl) {
                            cacheAvatar(url, dataUrl);
                            // Update image if it changed
                            if (img.src !== dataUrl) {
                                img.src = dataUrl;
                            }
                        }
                    });
                });
            }
            
            // Run on page load
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', processAvatars);
            } else {
                processAvatars();
            }
        })();
        
        // Modal helper
        function openModal(id) {
            const modal = document.getElementById(id);
            if (modal) {
                modal.classList.add('show');
            }
        }
        
        function closeModal(id) {
            const modal = document.getElementById(id);
            if (modal) {
                modal.classList.remove('show');
            }
        }
        
        // Close modal on outside click
        document.addEventListener('click', function(e) {
            if (e.target.classList.contains('modal')) {
                closeModal(e.target.id);
            }
        });
        
        // Close modal on Escape key
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                const modals = document.querySelectorAll('.modal.show');
                modals.forEach(modal => {
                    closeModal(modal.id);
                });
            }
        });
        
        // Highlight active nav item
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname;
            
            // Mobile nav items
            const mobileNavItems = document.querySelectorAll('.mobile-nav-item');
            mobileNavItems.forEach(item => {
                const href = item.getAttribute('href');
                if (href === currentPath || (currentPath === '/' && href === '/')) {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/feeds') && href === '/feeds') {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/reading-list') && href === '/reading-list') {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/profile') && href === '/profile') {
                    item.classList.add('active');
                }
            });
            
            // Sidebar nav items (desktop)
            const sidebarLinks = document.querySelectorAll('.sidebar-link');
            sidebarLinks.forEach(item => {
                const href = item.getAttribute('href');
                if (href === currentPath || (currentPath === '/' && href === '/')) {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/feeds') && href === '/feeds') {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/reading-list') && href === '/reading-list') {
                    item.classList.add('active');
                } else if (currentPath.startsWith('/profile') && href === '/profile') {
                    item.classList.add('active');
                }
            });
        });
    </script>
</body>
</html>
{{end}}
